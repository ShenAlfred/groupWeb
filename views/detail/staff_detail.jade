.about-detail.staff1.staffdetail
    .line
    .staff-img
        .staff-title
            h4.acontent #{content.data.name}
        .staff-container
            ul.staff-slide.clearfix
                each img in content.data.picUrlList
                    li
                        img(src="#{img}" alt="#{content.data.name}")
include ../include/page